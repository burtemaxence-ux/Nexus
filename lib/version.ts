/**
 * Version applicative affichée dans l'UI (footer landing, sidebar).
 * Source unique de vérité — garder synchronisé avec le champ "version" de package.json.
 */
export const APP_VERSION = '1.0.0'
